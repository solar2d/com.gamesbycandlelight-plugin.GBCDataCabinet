Sources for the plugin `plugin.GBCDataCabinet`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.GBCDataCabinet" = {
            publisherId = "com.gamesbycandlelight",
        },
    },
}
```
